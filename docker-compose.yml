services:
  mopidy:
    container_name: mopidy
    build:
      context: ./mopidy
    command: mopidy
    ports:
      - "6600:6600"
      - "6680:6680"
    volumes:
      - ./mopidy-data:/var/lib/mopidy:delegated
      - ./mopidy/config:/config:delegated
    environment:
      - MOPIDY_CLIENT_ID=${MOPIDY_CLIENT_ID}
      - MOPIDY_CLIENT_SECRET=${MOPIDY_CLIENT_SECRET}
